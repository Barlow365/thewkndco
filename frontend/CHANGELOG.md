# Changelog

All notable changes for this frontend workspace are documented in this file.

## [Unreleased]
- Add Tailwind CSS with PostCSS + Autoprefixer
- Add `components/Hero.tsx` — reusable UI section using Tailwind utilities
- Add `pages/tailwind-demo.tsx` — demo page with examples and dark-mode toggle
- Update `styles/globals.css` with Tailwind directives
- Enable class-based dark mode and add theme extensions in `tailwind.config.js`
 - Add site layout components (Navbar, Footer, Layout, Sections)
 - Add global components: Button, Container
 - Add Supabase client and starter auth pages (`/login`, `/signup`) plus `AuthProvider`
